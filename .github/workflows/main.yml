name: CI

on: [push]

jobs:
  build:
    name: build
    runs-on: self-hosted
    steps:
    - name: pull
      run: |
        cd /home/bs/MscWiki
        git pull
    - name: build
      run: gitbook build ./ /home/www/htdocs/wiki/
      
